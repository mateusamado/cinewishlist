class User {
  constructor(email, password, name, birthdate) {
    this.email = email;
    this.password = password;
    this.name = name;
    this.birthdate = birthdate;
  }
}

export default User;
